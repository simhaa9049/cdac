#include<stdio.h>
#include<math.h>
int main()
{
 int r,n,sum,temp;
 printf("enter a number:");
 scanf("%d",&n);
temp=n;
while(n!=0)
{
r=n%10;
sum=sum+r*r*r;
n=n/10;
}
if(sum==temp)
printf("%d is a armstrong num",temp);
else
printf("%d is  not a armstrong num",temp);
}
